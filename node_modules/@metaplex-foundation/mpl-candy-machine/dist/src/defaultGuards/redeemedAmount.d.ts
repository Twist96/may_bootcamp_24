import { RedeemedAmount, RedeemedAmountArgs } from '../generated';
import { GuardManifest } from '../guards';
/**
 * The redeemedAmount guard forbids minting when the
 * number of minted NFTs for the entire Candy Machine
 * reaches the configured maximum amount.
 */
export declare const redeemedAmountGuardManifest: GuardManifest<RedeemedAmountArgs, RedeemedAmount, RedeemedAmountMintArgs>;
export declare type RedeemedAmountMintArgs = Omit<RedeemedAmountArgs, 'limit'>;
