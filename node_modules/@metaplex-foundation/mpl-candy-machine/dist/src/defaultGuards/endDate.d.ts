import { EndDate, EndDateArgs } from '../generated';
import { GuardManifest } from '../guards';
/**
 * The endDate guard is used to specify a date to end the mint.
 * Any transaction received after the end date will fail.
 */
export declare const endDateGuardManifest: GuardManifest<EndDateArgs, EndDate>;
