import { StartDate, StartDateArgs } from '../generated';
import { GuardManifest } from '../guards';
/**
 * The startDate guard determines the start date of the mint.
 * Before this date, minting is not allowed.
 */
export declare const startDateGuardManifest: GuardManifest<StartDateArgs, StartDate>;
