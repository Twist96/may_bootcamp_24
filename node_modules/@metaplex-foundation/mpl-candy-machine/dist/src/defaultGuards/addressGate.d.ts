import { AddressGate, AddressGateArgs } from '../generated';
import { GuardManifest } from '../guards';
/**
 * The addressGate guard restricts the mint to a single
 * address which must match the minting wallet's address.
 */
export declare const addressGateGuardManifest: GuardManifest<AddressGateArgs, AddressGate>;
